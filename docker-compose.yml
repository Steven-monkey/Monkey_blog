services:
  web:
    build: .
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      SQLITE_PATH: /app/data/blog.db
    volumes:
      - sqlite-data:/app/data

volumes:
  sqlite-data:
